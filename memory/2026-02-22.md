- User directive: maintain strict focus mode; stay on task by default without needing repeated reminders.
- Enforcement directive (user confirmed): no soft ETAs; only report shipped checkpoints (feature + screenshot + commit), strict build blocks first, immediate blocker reporting, no drift.
